const defaultBreak = { minutes: 5, seconds: 0 };
const defaultSession = { minutes: 25, seconds: 0 };
const actions = {
  changeTime: true,
  color: false,
};

export { defaultBreak, defaultSession, actions };
